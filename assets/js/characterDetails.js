$origin_loc = document.getElementById('origin_loc');
$last_loc = document.getElementById('last_loc');

function setLocations(){
    /* $axios.get(`${basePath}/api/locations`).then(({ data }) => {
        toDo
    }); */
}
